package com.example.recipe.ui.adapters

import androidx.recyclerview.widget.DiffUtil
import com.example.recipe.models.recipes.ResponseRecipes

class xdgvxdgghf {
    private fun setData(items: List<ResponseRecipes.Result>,data: List<ResponseRecipes.Result>) {
        val adapterDiffUtils = BaseDiffUtils(items, data)
        val diffUtils = DiffUtil.calculateDiff(adapterDiffUtils)


    }    private fun setDataa(items: List<ResponseRecipes.Result>,data: List<ResponseRecipes.Result>) {
        val adapterDiffUtils = BaseDiffUtils(items, data)
        val diffUtils = DiffUtil.calculateDiff(adapterDiffUtils)


    }    private fun setDatad(items: List<ResponseRecipes.Result>,data: List<ResponseRecipes.Result>) {
        val adapterDiffUtils = BaseDiffUtils(items, data)
        val diffUtils = DiffUtil.calculateDiff(adapterDiffUtils)


    }    private fun setDataq(items: List<ResponseRecipes.Result>,data: List<ResponseRecipes.Result>) {
        val adapterDiffUtils = BaseDiffUtils(items, data)
        val diffUtils = DiffUtil.calculateDiff(adapterDiffUtils)


    }
}